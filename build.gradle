plugins {
    id 'java'
    id 'maven-publish'
}

group 'com.yourname'
version '1.0.0'

repositories {
    mavenCentral()
    maven { url 'https://files.minecraftforge.net/maven' }
}

dependencies {
    // Füge hier die Abhängigkeiten für Forge und andere Mods hinzu
}

jar {
    manifest {
        attributes(
            'Implementation-Title': 'EpicFight-IDF Compatibility',
            'Implementation-Version': version
        )
    }
}
